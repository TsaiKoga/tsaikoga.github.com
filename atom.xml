<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[TsaiKoga Blog]]></title>
  <link href="http://TsaiKoga.github.com/atom.xml" rel="self"/>
  <link href="http://TsaiKoga.github.com/"/>
  <updated>2013-07-29T23:48:47+08:00</updated>
  <id>http://TsaiKoga.github.com/</id>
  <author>
    <name><![CDATA[TsaiKoga]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[create octopress blog]]></title>
    <link href="http://TsaiKoga.github.com/blog/2013/07/28/create-octopress-blog/"/>
    <updated>2013-07-28T22:51:00+08:00</updated>
    <id>http://TsaiKoga.github.com/blog/2013/07/28/create-octopress-blog</id>
    <content type="html"><![CDATA[<h4>克隆octopress项目</h4>

<pre><code>git clone git://github.com/imathis/octopress.git blog
cd blog
</code></pre>

<p>查看Gemfile文件：
稍作修改，如：source <a href="http://ruby.taobao.org">http://ruby.taobao.org</a></p>

<pre><code>vim Gemfile
</code></pre>

<p>之后执行:</p>

<pre><code>bundle install
</code></pre>

<p>接着要做的事是rakefile文件的，查看：</p>

<pre><code>vim Rakefile    # 里面有许多rake任务
</code></pre>

<p>我们执行其中的:</p>

<pre><code>rake install # 复制theme/中的sass/和source/文件夹到blog/下
</code></pre>

<p>查看网页效果：</p>

<pre><code>rake preview
</code></pre>

<p>他会生成public文件夹</p>

<h4>创建仓库：名字要以用户名为头，区分大小写。(username.github.io)</h4>

<p>本地执行:</p>

<pre><code>rake setup_github_pages
</code></pre>

<p>当要填入url时，不要填入.git，这样生成的页面才在.com上,把public生成好的文件变成版本推送到远程仓库github。
说到在远程仓库做开发，就是将开发分支source覆盖原有的master分支。</p>

<pre><code>git checkout source
git push origin source
</code></pre>

<h4>如果想在source分支下有_deploy文件夹，只要</h4>

<pre><code>git clone https://github.com/TsaiKoga/tsaikoga.github.com.git
</code></pre>

<p>重新克隆了项目下来，再改变文件夹tsaikoga.github.com/名字为<em>deploy/,
修改</em>config.yml文件中的内容:一定要改url和/tsaikoga.github.com一样，</p>

<p>执行：</p>

<pre><code>rake generate
</code></pre>

<p>再执行：</p>

<pre><code>rake deploy
</code></pre>

<p>部署到远程仓库，到网址刷新看看。</p>

<h4>注册一个google analytics帐号：</h4>

<p>生成tracking_id:</p>

<h4>新建一个博客：</h4>

<pre><code>rake new_post["Hello TsaiKoga"]
</code></pre>

<p>存放到了source/<em>post之中,
编辑</em>post文件，如果使用disqus，这里的comment必须填true，下面有原因。</p>

<h4>做评论，可以基于第三方插件：disqus</h4>

<p>扯开一下话题：</p>

<blockquote><p>Disqus的主要目标是通过提供功能强大的第三评论系统，将当前不同网站的相对孤立、隔绝的评论系统，连接成具有社会化特性的大网，用户使用Disqus，在不同网站上评论，无需重复注册账号，只需使用Disqus账号或者<em>第三方平台账号</em>，即可方便的进行评论，且所有评论都会存储、保存在Disqus账号后台，方便随时查看、回顾。
可以看到source/<em>layouts/post.html文件中已经有这个插件的评论方法：
在</em>config.yml中加入你申请的disqus的short_name</p></blockquote>
]]></content>
  </entry>
  
</feed>
